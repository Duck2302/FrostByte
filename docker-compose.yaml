services:
  master:
    image: master-node
    ports:
      - "8080:8080"
    networks:
      - mynetwork

  worker:
    image: worker-node
    deploy:
      replicas: 5
    networks:
      - mynetwork

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    networks:
      - mynetwork

networks:
  mynetwork: